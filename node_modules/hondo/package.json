{
  "name": "hondo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@12.0.2",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run build && npm run test --workspaces --if-present",
    "typecheck": "npm run build && npm run typecheck --workspaces --if-present",
    "pack:check": "npm run build && npm pack --dry-run --workspace @hondo/core && npm pack --dry-run --workspace @hondo/solid"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "5.9.2",
    "vitest": "4.1.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
