name: Fix and Apply LSP Editor Integration

on:
  push:
    branches: [feature/lsp-phase2]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/lsp-phase2
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - name: Patch known compile blockers
        run: python3 .github/fix_and_apply_lsp_editor.py
      - name: Apply guarded editor integration
        run: python3 .github/apply_lsp_editor.py
      - name: Format Zig sources
        run: zig fmt src build.zig
      - name: Verify editor and LSP core
        run: zig build test-core -Dheadless-only=true
      - name: Remove one-shot helpers
        run: rm .github/apply_lsp_editor.py .github/fix_and_apply_lsp_editor.py .github/workflows/apply-lsp-editor.yml .github/workflows/fix-and-apply-lsp-editor.yml
      - name: Commit verified integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/editor.zig src/lsp/client.zig src/lsp_bridge.zig .github/apply_lsp_editor.py .github/fix_and_apply_lsp_editor.py .github/workflows/apply-lsp-editor.yml .github/workflows/fix-and-apply-lsp-editor.yml
          git commit -m "feat(lsp): wire semantic intelligence into editor"
          git push origin HEAD:feature/lsp-phase2
